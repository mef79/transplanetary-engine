// import { fromJS } from 'immutable'
// import { makeSelectGameDomain } from '../selectors'

// const selector = makeSelectGameDomain()

describe('makeSelectGameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
