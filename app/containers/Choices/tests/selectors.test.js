// import { fromJS } from 'immutable'
// import { makeSelectChoicesDomain } from '../selectors'

// const selector = makeSelectChoicesDomain()

describe('makeSelectChoicesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
