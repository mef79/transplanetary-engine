// import { fromJS } from 'immutable'
// import { makeSelectMenuDomain } from '../selectors'

// const selector = makeSelectMenuDomain()

describe('makeSelectMenuDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
