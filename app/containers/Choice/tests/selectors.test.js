// import { fromJS } from 'immutable'
// import { makeSelectChoiceDomain } from '../selectors'

// const selector = makeSelectChoiceDomain()

describe('makeSelectChoiceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
