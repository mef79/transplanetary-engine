// import { fromJS } from 'immutable'
// import { makeSelectTitlePageDomain } from '../selectors'

// const selector = makeSelectTitlePageDomain()

describe('makeSelectTitlePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
